// @version=5
// Auto-generated by cycle_5h at 2026-06-08 14:29 UTC+5
// Top-3 strategies from latest sweep

indicator("FOREX Auto-Strategy [2026-06-08 14:29 UTC+5]", overlay=true)

// === 1. GBPUSD — WR 55.89% · 13.6 trades/day ===
s1_adx_min = input.float(12, "#1 GBPUSD ADX min")
s1_adx_max = input.float(50.0, "#1 GBPUSD ADX max")
s1_conf_min = input.int(75, "#1 GBPUSD Min Confidence")
s1_tq_min = input.int(65, "#1 GBPUSD Min Trend Quality")
s1_horizon = input.int(180, "#1 GBPUSD Horizon (min)")
s1_require_mtf = input.bool(true, "#1 GBPUSD Require MTF")

// Current signal: SELL (confidence 92%, ADX 40)

// === 2. NZDCAD — WR 54.72% · 19.8 trades/day ===
s2_adx_min = input.float(12, "#2 NZDCAD ADX min")
s2_adx_max = input.float(50.0, "#2 NZDCAD ADX max")
s2_conf_min = input.int(65, "#2 NZDCAD Min Confidence")
s2_tq_min = input.int(65, "#2 NZDCAD Min Trend Quality")
s2_horizon = input.int(240, "#2 NZDCAD Horizon (min)")
s2_require_mtf = input.bool(false, "#2 NZDCAD Require MTF")

// Current signal: SELL (confidence 84%, ADX 43)

// === 3. EURCAD — WR 53.63% · 14.8 trades/day ===
s3_adx_min = input.float(12, "#3 EURCAD ADX min")
s3_adx_max = input.float(50.0, "#3 EURCAD ADX max")
s3_conf_min = input.int(75, "#3 EURCAD Min Confidence")
s3_tq_min = input.int(65, "#3 EURCAD Min Trend Quality")
s3_horizon = input.int(60, "#3 EURCAD Horizon (min)")
s3_require_mtf = input.bool(false, "#3 EURCAD Require MTF")

// Current signal: SELL (confidence 87%, ADX 31)

// --- Live Signal Logic (pair #1) ---
adx_len = input.int(14, "ADX Length")
[diPlus, diMinus, adxVal] = ta.dmi(adx_len, adx_len)
ema_fast = ta.ema(close, 8)
ema_slow = ta.ema(close, 21)

bull_signal = adxVal >= s1_adx_min and adxVal <= s1_adx_max and ema_fast > ema_slow and diPlus > diMinus
bear_signal = adxVal >= s1_adx_min and adxVal <= s1_adx_max and ema_fast < ema_slow and diMinus > diPlus

plotshape(bull_signal, style=shape.triangleup, location=location.belowbar, color=color.green, size=size.small, title="BUY")
plotshape(bear_signal, style=shape.triangledown, location=location.abovebar, color=color.red, size=size.small, title="SELL")

// WR: 55.89% on 365-day backtest
// Trades/day: 13.6
// Pair: GBPUSD
