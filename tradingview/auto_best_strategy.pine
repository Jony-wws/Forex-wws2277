// @version=5
// Auto-generated by cycle_5h at 2026-05-11 17:44 UTC+5
// Top-3 strategies from latest sweep

indicator("FOREX Auto-Strategy [2026-05-11 17:44 UTC+5]", overlay=true)

// === 1. NZDJPY — WR 52.08% · 17.9 trades/day ===
s1_adx_min = input.float(12, "#1 NZDJPY ADX min")
s1_adx_max = input.float(50.0, "#1 NZDJPY ADX max")
s1_conf_min = input.int(75, "#1 NZDJPY Min Confidence")
s1_tq_min = input.int(65, "#1 NZDJPY Min Trend Quality")
s1_horizon = input.int(60, "#1 NZDJPY Horizon (min)")
s1_require_mtf = input.bool(false, "#1 NZDJPY Require MTF")

// Current signal: WAIT (confidence 89%, ADX 10)

// === 2. AUDNZD — WR 71.73% · 5.6 trades/day ===
s2_adx_min = input.float(18, "#2 AUDNZD ADX min")
s2_adx_max = input.float(60.0, "#2 AUDNZD ADX max")
s2_conf_min = input.int(72, "#2 AUDNZD Min Confidence")
s2_tq_min = input.int(75, "#2 AUDNZD Min Trend Quality")
s2_horizon = input.int(420, "#2 AUDNZD Horizon (min)")
s2_require_mtf = input.bool(true, "#2 AUDNZD Require MTF")

// Current signal: WAIT (confidence 88%, ADX 22)

// === 3. USDCHF — WR 52.82% · 20.3 trades/day ===
s3_adx_min = input.float(12, "#3 USDCHF ADX min")
s3_adx_max = input.float(50.0, "#3 USDCHF ADX max")
s3_conf_min = input.int(75, "#3 USDCHF Min Confidence")
s3_tq_min = input.int(50, "#3 USDCHF Min Trend Quality")
s3_horizon = input.int(120, "#3 USDCHF Horizon (min)")
s3_require_mtf = input.bool(true, "#3 USDCHF Require MTF")

// Current signal: WAIT (confidence 69%, ADX 18)

// --- Live Signal Logic (pair #1) ---
adx_len = input.int(14, "ADX Length")
[diPlus, diMinus, adxVal] = ta.dmi(adx_len, adx_len)
ema_fast = ta.ema(close, 8)
ema_slow = ta.ema(close, 21)

bull_signal = adxVal >= s1_adx_min and adxVal <= s1_adx_max and ema_fast > ema_slow and diPlus > diMinus
bear_signal = adxVal >= s1_adx_min and adxVal <= s1_adx_max and ema_fast < ema_slow and diMinus > diPlus

plotshape(bull_signal, style=shape.triangleup, location=location.belowbar, color=color.green, size=size.small, title="BUY")
plotshape(bear_signal, style=shape.triangledown, location=location.abovebar, color=color.red, size=size.small, title="SELL")

// WR: 52.08% on 365-day backtest
// Trades/day: 17.9
// Pair: NZDJPY
