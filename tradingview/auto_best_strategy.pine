// @version=5
// Auto-generated by cycle_5h at 2026-06-11 09:25 UTC+5
// Top-3 strategies from latest sweep

indicator("FOREX Auto-Strategy [2026-06-11 09:25 UTC+5]", overlay=true)

// === 1. AUDNZD — WR 64.62% · 11.0 trades/day ===
s1_adx_min = input.float(18, "#1 AUDNZD ADX min")
s1_adx_max = input.float(60.0, "#1 AUDNZD ADX max")
s1_conf_min = input.int(72, "#1 AUDNZD Min Confidence")
s1_tq_min = input.int(65, "#1 AUDNZD Min Trend Quality")
s1_horizon = input.int(300, "#1 AUDNZD Horizon (min)")
s1_require_mtf = input.bool(true, "#1 AUDNZD Require MTF")

// Current signal: WAIT (confidence 72%, ADX 45)

// === 2. NZDJPY — WR 58.74% · 5.0 trades/day ===
s2_adx_min = input.float(18, "#2 NZDJPY ADX min")
s2_adx_max = input.float(60.0, "#2 NZDJPY ADX max")
s2_conf_min = input.int(72, "#2 NZDJPY Min Confidence")
s2_tq_min = input.int(75, "#2 NZDJPY Min Trend Quality")
s2_horizon = input.int(300, "#2 NZDJPY Horizon (min)")
s2_require_mtf = input.bool(true, "#2 NZDJPY Require MTF")

// Current signal: WAIT (confidence 91%, ADX 16)

// === 3. GBPCHF — WR 55.1% · 19.0 trades/day ===
s3_adx_min = input.float(12, "#3 GBPCHF ADX min")
s3_adx_max = input.float(50.0, "#3 GBPCHF ADX max")
s3_conf_min = input.int(75, "#3 GBPCHF Min Confidence")
s3_tq_min = input.int(60, "#3 GBPCHF Min Trend Quality")
s3_horizon = input.int(300, "#3 GBPCHF Horizon (min)")
s3_require_mtf = input.bool(false, "#3 GBPCHF Require MTF")

// Current signal: WAIT (confidence 64%, ADX 23)

// --- Live Signal Logic (pair #1) ---
adx_len = input.int(14, "ADX Length")
[diPlus, diMinus, adxVal] = ta.dmi(adx_len, adx_len)
ema_fast = ta.ema(close, 8)
ema_slow = ta.ema(close, 21)

bull_signal = adxVal >= s1_adx_min and adxVal <= s1_adx_max and ema_fast > ema_slow and diPlus > diMinus
bear_signal = adxVal >= s1_adx_min and adxVal <= s1_adx_max and ema_fast < ema_slow and diMinus > diPlus

plotshape(bull_signal, style=shape.triangleup, location=location.belowbar, color=color.green, size=size.small, title="BUY")
plotshape(bear_signal, style=shape.triangledown, location=location.abovebar, color=color.red, size=size.small, title="SELL")

// WR: 64.62% on 365-day backtest
// Trades/day: 11.0
// Pair: AUDNZD
