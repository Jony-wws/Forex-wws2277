// @version=5
// Auto-generated by cycle_5h at 2026-06-05 17:43 UTC+5
// Top-3 strategies from latest sweep

indicator("FOREX Auto-Strategy [2026-06-05 17:43 UTC+5]", overlay=true)

// === 1. NZDUSD — WR 55.41% · 26.0 trades/day ===
s1_adx_min = input.float(12, "#1 NZDUSD ADX min")
s1_adx_max = input.float(50.0, "#1 NZDUSD ADX max")
s1_conf_min = input.int(70, "#1 NZDUSD Min Confidence")
s1_tq_min = input.int(65, "#1 NZDUSD Min Trend Quality")
s1_horizon = input.int(240, "#1 NZDUSD Horizon (min)")
s1_require_mtf = input.bool(false, "#1 NZDUSD Require MTF")

// Current signal: WAIT (confidence 88%, ADX 21)

// === 2. EURAUD — WR 54.29% · 11.8 trades/day ===
s2_adx_min = input.float(12, "#2 EURAUD ADX min")
s2_adx_max = input.float(50.0, "#2 EURAUD ADX max")
s2_conf_min = input.int(75, "#2 EURAUD Min Confidence")
s2_tq_min = input.int(65, "#2 EURAUD Min Trend Quality")
s2_horizon = input.int(60, "#2 EURAUD Horizon (min)")
s2_require_mtf = input.bool(true, "#2 EURAUD Require MTF")

// Current signal: WAIT (confidence 51%, ADX 26)

// === 3. EURCAD — WR 53.39% · 14.7 trades/day ===
s3_adx_min = input.float(12, "#3 EURCAD ADX min")
s3_adx_max = input.float(50.0, "#3 EURCAD ADX max")
s3_conf_min = input.int(75, "#3 EURCAD Min Confidence")
s3_tq_min = input.int(65, "#3 EURCAD Min Trend Quality")
s3_horizon = input.int(60, "#3 EURCAD Horizon (min)")
s3_require_mtf = input.bool(false, "#3 EURCAD Require MTF")

// Current signal: WAIT (confidence 86%, ADX 16)

// --- Live Signal Logic (pair #1) ---
adx_len = input.int(14, "ADX Length")
[diPlus, diMinus, adxVal] = ta.dmi(adx_len, adx_len)
ema_fast = ta.ema(close, 8)
ema_slow = ta.ema(close, 21)

bull_signal = adxVal >= s1_adx_min and adxVal <= s1_adx_max and ema_fast > ema_slow and diPlus > diMinus
bear_signal = adxVal >= s1_adx_min and adxVal <= s1_adx_max and ema_fast < ema_slow and diMinus > diPlus

plotshape(bull_signal, style=shape.triangleup, location=location.belowbar, color=color.green, size=size.small, title="BUY")
plotshape(bear_signal, style=shape.triangledown, location=location.abovebar, color=color.red, size=size.small, title="SELL")

// WR: 55.41% on 365-day backtest
// Trades/day: 26.0
// Pair: NZDUSD
