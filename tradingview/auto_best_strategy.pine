// @version=5
// Auto-generated by cycle_5h at 2026-05-19 13:34 UTC+5
// Top-3 strategies from latest sweep

indicator("FOREX Auto-Strategy [2026-05-19 13:34 UTC+5]", overlay=true)

// === 1. GBPCHF — WR 60.89% · 3.3 trades/day ===
s1_adx_min = input.float(18, "#1 GBPCHF ADX min")
s1_adx_max = input.float(60.0, "#1 GBPCHF ADX max")
s1_conf_min = input.int(72, "#1 GBPCHF Min Confidence")
s1_tq_min = input.int(75, "#1 GBPCHF Min Trend Quality")
s1_horizon = input.int(420, "#1 GBPCHF Horizon (min)")
s1_require_mtf = input.bool(true, "#1 GBPCHF Require MTF")

// Current signal: WAIT (confidence 64%, ADX 32)

// === 2. NZDUSD — WR 52.42% · 24.4 trades/day ===
s2_adx_min = input.float(12, "#2 NZDUSD ADX min")
s2_adx_max = input.float(50.0, "#2 NZDUSD ADX max")
s2_conf_min = input.int(65, "#2 NZDUSD Min Confidence")
s2_tq_min = input.int(65, "#2 NZDUSD Min Trend Quality")
s2_horizon = input.int(240, "#2 NZDUSD Horizon (min)")
s2_require_mtf = input.bool(false, "#2 NZDUSD Require MTF")

// Current signal: SELL (confidence 92%, ADX 23)

// === 3. EURUSD — WR 51.86% · 28.1 trades/day ===
s3_adx_min = input.float(12, "#3 EURUSD ADX min")
s3_adx_max = input.float(50.0, "#3 EURUSD ADX max")
s3_conf_min = input.int(65, "#3 EURUSD Min Confidence")
s3_tq_min = input.int(60, "#3 EURUSD Min Trend Quality")
s3_horizon = input.int(240, "#3 EURUSD Horizon (min)")
s3_require_mtf = input.bool(false, "#3 EURUSD Require MTF")

// Current signal: WAIT (confidence 92%, ADX 20)

// --- Live Signal Logic (pair #1) ---
adx_len = input.int(14, "ADX Length")
[diPlus, diMinus, adxVal] = ta.dmi(adx_len, adx_len)
ema_fast = ta.ema(close, 8)
ema_slow = ta.ema(close, 21)

bull_signal = adxVal >= s1_adx_min and adxVal <= s1_adx_max and ema_fast > ema_slow and diPlus > diMinus
bear_signal = adxVal >= s1_adx_min and adxVal <= s1_adx_max and ema_fast < ema_slow and diMinus > diPlus

plotshape(bull_signal, style=shape.triangleup, location=location.belowbar, color=color.green, size=size.small, title="BUY")
plotshape(bear_signal, style=shape.triangledown, location=location.abovebar, color=color.red, size=size.small, title="SELL")

// WR: 60.89% on 365-day backtest
// Trades/day: 3.3
// Pair: GBPCHF
