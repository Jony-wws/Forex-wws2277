// @version=5
// Auto-generated by cycle_5h at 2026-05-21 13:38 UTC+5
// Top-3 strategies from latest sweep

indicator("FOREX Auto-Strategy [2026-05-21 13:38 UTC+5]", overlay=true)

// === 1. EURCAD — WR 56.17% · 6.3 trades/day ===
s1_adx_min = input.float(18, "#1 EURCAD ADX min")
s1_adx_max = input.float(60.0, "#1 EURCAD ADX max")
s1_conf_min = input.int(72, "#1 EURCAD Min Confidence")
s1_tq_min = input.int(75, "#1 EURCAD Min Trend Quality")
s1_horizon = input.int(420, "#1 EURCAD Horizon (min)")
s1_require_mtf = input.bool(true, "#1 EURCAD Require MTF")

// Current signal: WAIT (confidence 90%, ADX 16)

// === 2. EURGBP — WR 54.06% · 10.7 trades/day ===
s2_adx_min = input.float(12, "#2 EURGBP ADX min")
s2_adx_max = input.float(50.0, "#2 EURGBP ADX max")
s2_conf_min = input.int(75, "#2 EURGBP Min Confidence")
s2_tq_min = input.int(65, "#2 EURGBP Min Trend Quality")
s2_horizon = input.int(60, "#2 EURGBP Horizon (min)")
s2_require_mtf = input.bool(false, "#2 EURGBP Require MTF")

// Current signal: WAIT (confidence 56%, ADX 27)

// === 3. NZDCAD — WR 54.62% · 40.4 trades/day ===
s3_adx_min = input.float(12, "#3 NZDCAD ADX min")
s3_adx_max = input.float(50.0, "#3 NZDCAD ADX max")
s3_conf_min = input.int(65, "#3 NZDCAD Min Confidence")
s3_tq_min = input.int(50, "#3 NZDCAD Min Trend Quality")
s3_horizon = input.int(60, "#3 NZDCAD Horizon (min)")
s3_require_mtf = input.bool(false, "#3 NZDCAD Require MTF")

// Current signal: WAIT (confidence 89%, ADX 15)

// --- Live Signal Logic (pair #1) ---
adx_len = input.int(14, "ADX Length")
[diPlus, diMinus, adxVal] = ta.dmi(adx_len, adx_len)
ema_fast = ta.ema(close, 8)
ema_slow = ta.ema(close, 21)

bull_signal = adxVal >= s1_adx_min and adxVal <= s1_adx_max and ema_fast > ema_slow and diPlus > diMinus
bear_signal = adxVal >= s1_adx_min and adxVal <= s1_adx_max and ema_fast < ema_slow and diMinus > diPlus

plotshape(bull_signal, style=shape.triangleup, location=location.belowbar, color=color.green, size=size.small, title="BUY")
plotshape(bear_signal, style=shape.triangledown, location=location.abovebar, color=color.red, size=size.small, title="SELL")

// WR: 56.17% on 365-day backtest
// Trades/day: 6.3
// Pair: EURCAD
