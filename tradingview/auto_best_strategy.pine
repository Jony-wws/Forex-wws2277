// @version=5
// Auto-generated by cycle_5h at 2026-05-12 01:51 UTC+5
// Top-3 strategies from latest sweep

indicator("FOREX Auto-Strategy [2026-05-12 01:51 UTC+5]", overlay=true)

// === 1. AUDNZD — WR 71.79% · 5.6 trades/day ===
s1_adx_min = input.float(18, "#1 AUDNZD ADX min")
s1_adx_max = input.float(60.0, "#1 AUDNZD ADX max")
s1_conf_min = input.int(72, "#1 AUDNZD Min Confidence")
s1_tq_min = input.int(75, "#1 AUDNZD Min Trend Quality")
s1_horizon = input.int(420, "#1 AUDNZD Horizon (min)")
s1_require_mtf = input.bool(true, "#1 AUDNZD Require MTF")

// Current signal: WAIT (confidence 59%, ADX 17)

// === 2. NZDJPY — WR 52.08% · 18.2 trades/day ===
s2_adx_min = input.float(12, "#2 NZDJPY ADX min")
s2_adx_max = input.float(50.0, "#2 NZDJPY ADX max")
s2_conf_min = input.int(75, "#2 NZDJPY Min Confidence")
s2_tq_min = input.int(65, "#2 NZDJPY Min Trend Quality")
s2_horizon = input.int(60, "#2 NZDJPY Horizon (min)")
s2_require_mtf = input.bool(false, "#2 NZDJPY Require MTF")

// Current signal: BUY (confidence 92%, ADX 21)

// === 3. GBPUSD — WR 54.08% · 17.3 trades/day ===
s3_adx_min = input.float(12, "#3 GBPUSD ADX min")
s3_adx_max = input.float(50.0, "#3 GBPUSD ADX max")
s3_conf_min = input.int(75, "#3 GBPUSD Min Confidence")
s3_tq_min = input.int(60, "#3 GBPUSD Min Trend Quality")
s3_horizon = input.int(60, "#3 GBPUSD Horizon (min)")
s3_require_mtf = input.bool(true, "#3 GBPUSD Require MTF")

// Current signal: WAIT (confidence 63%, ADX 21)

// --- Live Signal Logic (pair #1) ---
adx_len = input.int(14, "ADX Length")
[diPlus, diMinus, adxVal] = ta.dmi(adx_len, adx_len)
ema_fast = ta.ema(close, 8)
ema_slow = ta.ema(close, 21)

bull_signal = adxVal >= s1_adx_min and adxVal <= s1_adx_max and ema_fast > ema_slow and diPlus > diMinus
bear_signal = adxVal >= s1_adx_min and adxVal <= s1_adx_max and ema_fast < ema_slow and diMinus > diPlus

plotshape(bull_signal, style=shape.triangleup, location=location.belowbar, color=color.green, size=size.small, title="BUY")
plotshape(bear_signal, style=shape.triangledown, location=location.abovebar, color=color.red, size=size.small, title="SELL")

// WR: 71.79% on 365-day backtest
// Trades/day: 5.6
// Pair: AUDNZD
