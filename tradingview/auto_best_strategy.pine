// @version=5
// Auto-generated by cycle_5h at 2026-05-29 13:48 UTC+5
// Top-3 strategies from latest sweep

indicator("FOREX Auto-Strategy [2026-05-29 13:48 UTC+5]", overlay=true)

// === 1. GBPNZD — WR 55.93% · 14.7 trades/day ===
s1_adx_min = input.float(12, "#1 GBPNZD ADX min")
s1_adx_max = input.float(50.0, "#1 GBPNZD ADX max")
s1_conf_min = input.int(75, "#1 GBPNZD Min Confidence")
s1_tq_min = input.int(65, "#1 GBPNZD Min Trend Quality")
s1_horizon = input.int(180, "#1 GBPNZD Horizon (min)")
s1_require_mtf = input.bool(false, "#1 GBPNZD Require MTF")

// Current signal: SELL (confidence 92%, ADX 67)

// === 2. NZDUSD — WR 55.42% · 23.2 trades/day ===
s2_adx_min = input.float(12, "#2 NZDUSD ADX min")
s2_adx_max = input.float(50.0, "#2 NZDUSD ADX max")
s2_conf_min = input.int(65, "#2 NZDUSD Min Confidence")
s2_tq_min = input.int(50, "#2 NZDUSD Min Trend Quality")
s2_horizon = input.int(300, "#2 NZDUSD Horizon (min)")
s2_require_mtf = input.bool(true, "#2 NZDUSD Require MTF")

// Current signal: BUY (confidence 92%, ADX 48)

// === 3. AUDNZD — WR 68.63% · 6.3 trades/day ===
s3_adx_min = input.float(18, "#3 AUDNZD ADX min")
s3_adx_max = input.float(60.0, "#3 AUDNZD ADX max")
s3_conf_min = input.int(72, "#3 AUDNZD Min Confidence")
s3_tq_min = input.int(75, "#3 AUDNZD Min Trend Quality")
s3_horizon = input.int(300, "#3 AUDNZD Horizon (min)")
s3_require_mtf = input.bool(true, "#3 AUDNZD Require MTF")

// Current signal: SELL (confidence 92%, ADX 58)

// --- Live Signal Logic (pair #1) ---
adx_len = input.int(14, "ADX Length")
[diPlus, diMinus, adxVal] = ta.dmi(adx_len, adx_len)
ema_fast = ta.ema(close, 8)
ema_slow = ta.ema(close, 21)

bull_signal = adxVal >= s1_adx_min and adxVal <= s1_adx_max and ema_fast > ema_slow and diPlus > diMinus
bear_signal = adxVal >= s1_adx_min and adxVal <= s1_adx_max and ema_fast < ema_slow and diMinus > diPlus

plotshape(bull_signal, style=shape.triangleup, location=location.belowbar, color=color.green, size=size.small, title="BUY")
plotshape(bear_signal, style=shape.triangledown, location=location.abovebar, color=color.red, size=size.small, title="SELL")

// WR: 55.93% on 365-day backtest
// Trades/day: 14.7
// Pair: GBPNZD
