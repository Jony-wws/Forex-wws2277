// @version=5
// Auto-generated by cycle_5h at 2026-06-08 23:02 UTC+5
// Top-3 strategies from latest sweep

indicator("FOREX Auto-Strategy [2026-06-08 23:02 UTC+5]", overlay=true)

// === 1. EURCAD — WR 53.68% · 14.8 trades/day ===
s1_adx_min = input.float(12, "#1 EURCAD ADX min")
s1_adx_max = input.float(50.0, "#1 EURCAD ADX max")
s1_conf_min = input.int(75, "#1 EURCAD Min Confidence")
s1_tq_min = input.int(65, "#1 EURCAD Min Trend Quality")
s1_horizon = input.int(60, "#1 EURCAD Horizon (min)")
s1_require_mtf = input.bool(false, "#1 EURCAD Require MTF")

// Current signal: WAIT (confidence 81%, ADX 24)

// === 2. GBPUSD — WR 57.28% · 7.5 trades/day ===
s2_adx_min = input.float(18, "#2 GBPUSD ADX min")
s2_adx_max = input.float(60.0, "#2 GBPUSD ADX max")
s2_conf_min = input.int(72, "#2 GBPUSD Min Confidence")
s2_tq_min = input.int(75, "#2 GBPUSD Min Trend Quality")
s2_horizon = input.int(300, "#2 GBPUSD Horizon (min)")
s2_require_mtf = input.bool(true, "#2 GBPUSD Require MTF")

// Current signal: WAIT (confidence 74%, ADX 26)

// === 3. CADCHF — WR 54.43% · 9.7 trades/day ===
s3_adx_min = input.float(18, "#3 CADCHF ADX min")
s3_adx_max = input.float(60.0, "#3 CADCHF ADX max")
s3_conf_min = input.int(72, "#3 CADCHF Min Confidence")
s3_tq_min = input.int(65, "#3 CADCHF Min Trend Quality")
s3_horizon = input.int(420, "#3 CADCHF Horizon (min)")
s3_require_mtf = input.bool(true, "#3 CADCHF Require MTF")

// Current signal: WAIT (confidence 82%, ADX 36)

// --- Live Signal Logic (pair #1) ---
adx_len = input.int(14, "ADX Length")
[diPlus, diMinus, adxVal] = ta.dmi(adx_len, adx_len)
ema_fast = ta.ema(close, 8)
ema_slow = ta.ema(close, 21)

bull_signal = adxVal >= s1_adx_min and adxVal <= s1_adx_max and ema_fast > ema_slow and diPlus > diMinus
bear_signal = adxVal >= s1_adx_min and adxVal <= s1_adx_max and ema_fast < ema_slow and diMinus > diPlus

plotshape(bull_signal, style=shape.triangleup, location=location.belowbar, color=color.green, size=size.small, title="BUY")
plotshape(bear_signal, style=shape.triangledown, location=location.abovebar, color=color.red, size=size.small, title="SELL")

// WR: 53.68% on 365-day backtest
// Trades/day: 14.8
// Pair: EURCAD
