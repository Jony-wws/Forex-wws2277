// @version=5
// Auto-generated by cycle_5h at 2026-06-03 19:49 UTC+5
// Top-3 strategies from latest sweep

indicator("FOREX Auto-Strategy [2026-06-03 19:49 UTC+5]", overlay=true)

// === 1. EURAUD — WR 54.13% · 11.7 trades/day ===
s1_adx_min = input.float(12, "#1 EURAUD ADX min")
s1_adx_max = input.float(50.0, "#1 EURAUD ADX max")
s1_conf_min = input.int(75, "#1 EURAUD Min Confidence")
s1_tq_min = input.int(65, "#1 EURAUD Min Trend Quality")
s1_horizon = input.int(60, "#1 EURAUD Horizon (min)")
s1_require_mtf = input.bool(true, "#1 EURAUD Require MTF")

// Current signal: WAIT (confidence 82%, ADX 17)

// === 2. EURCHF — WR 57.24% · 12.1 trades/day ===
s2_adx_min = input.float(12, "#2 EURCHF ADX min")
s2_adx_max = input.float(50.0, "#2 EURCHF ADX max")
s2_conf_min = input.int(65, "#2 EURCHF Min Confidence")
s2_tq_min = input.int(65, "#2 EURCHF Min Trend Quality")
s2_horizon = input.int(60, "#2 EURCHF Horizon (min)")
s2_require_mtf = input.bool(false, "#2 EURCHF Require MTF")

// Current signal: WAIT (confidence 92%, ADX 31)

// === 3. GBPCHF — WR 54.52% · 18.3 trades/day ===
s3_adx_min = input.float(12, "#3 GBPCHF ADX min")
s3_adx_max = input.float(50.0, "#3 GBPCHF ADX max")
s3_conf_min = input.int(75, "#3 GBPCHF Min Confidence")
s3_tq_min = input.int(60, "#3 GBPCHF Min Trend Quality")
s3_horizon = input.int(60, "#3 GBPCHF Horizon (min)")
s3_require_mtf = input.bool(false, "#3 GBPCHF Require MTF")

// Current signal: BUY (confidence 91%, ADX 33)

// --- Live Signal Logic (pair #1) ---
adx_len = input.int(14, "ADX Length")
[diPlus, diMinus, adxVal] = ta.dmi(adx_len, adx_len)
ema_fast = ta.ema(close, 8)
ema_slow = ta.ema(close, 21)

bull_signal = adxVal >= s1_adx_min and adxVal <= s1_adx_max and ema_fast > ema_slow and diPlus > diMinus
bear_signal = adxVal >= s1_adx_min and adxVal <= s1_adx_max and ema_fast < ema_slow and diMinus > diPlus

plotshape(bull_signal, style=shape.triangleup, location=location.belowbar, color=color.green, size=size.small, title="BUY")
plotshape(bear_signal, style=shape.triangledown, location=location.abovebar, color=color.red, size=size.small, title="SELL")

// WR: 54.13% on 365-day backtest
// Trades/day: 11.7
// Pair: EURAUD
