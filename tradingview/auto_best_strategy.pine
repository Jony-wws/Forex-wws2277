// @version=5
// Auto-generated by cycle_5h at 2026-06-09 13:36 UTC+5
// Top-3 strategies from latest sweep

indicator("FOREX Auto-Strategy [2026-06-09 13:36 UTC+5]", overlay=true)

// === 1. GBPUSD — WR 55.94% · 13.8 trades/day ===
s1_adx_min = input.float(12, "#1 GBPUSD ADX min")
s1_adx_max = input.float(50.0, "#1 GBPUSD ADX max")
s1_conf_min = input.int(75, "#1 GBPUSD Min Confidence")
s1_tq_min = input.int(65, "#1 GBPUSD Min Trend Quality")
s1_horizon = input.int(180, "#1 GBPUSD Horizon (min)")
s1_require_mtf = input.bool(true, "#1 GBPUSD Require MTF")

// Current signal: WAIT (confidence 87%, ADX 21)

// === 2. USDCHF — WR 53.36% · 21.9 trades/day ===
s2_adx_min = input.float(12, "#2 USDCHF ADX min")
s2_adx_max = input.float(50.0, "#2 USDCHF ADX max")
s2_conf_min = input.int(65, "#2 USDCHF Min Confidence")
s2_tq_min = input.int(50, "#2 USDCHF Min Trend Quality")
s2_horizon = input.int(240, "#2 USDCHF Horizon (min)")
s2_require_mtf = input.bool(true, "#2 USDCHF Require MTF")

// Current signal: WAIT (confidence 56%, ADX 16)

// === 3. CHFJPY — WR 52.94% · 23.8 trades/day ===
s3_adx_min = input.float(12, "#3 CHFJPY ADX min")
s3_adx_max = input.float(50.0, "#3 CHFJPY ADX max")
s3_conf_min = input.int(75, "#3 CHFJPY Min Confidence")
s3_tq_min = input.int(60, "#3 CHFJPY Min Trend Quality")
s3_horizon = input.int(180, "#3 CHFJPY Horizon (min)")
s3_require_mtf = input.bool(false, "#3 CHFJPY Require MTF")

// Current signal: WAIT (confidence 56%, ADX 22)

// --- Live Signal Logic (pair #1) ---
adx_len = input.int(14, "ADX Length")
[diPlus, diMinus, adxVal] = ta.dmi(adx_len, adx_len)
ema_fast = ta.ema(close, 8)
ema_slow = ta.ema(close, 21)

bull_signal = adxVal >= s1_adx_min and adxVal <= s1_adx_max and ema_fast > ema_slow and diPlus > diMinus
bear_signal = adxVal >= s1_adx_min and adxVal <= s1_adx_max and ema_fast < ema_slow and diMinus > diPlus

plotshape(bull_signal, style=shape.triangleup, location=location.belowbar, color=color.green, size=size.small, title="BUY")
plotshape(bear_signal, style=shape.triangledown, location=location.abovebar, color=color.red, size=size.small, title="SELL")

// WR: 55.94% on 365-day backtest
// Trades/day: 13.8
// Pair: GBPUSD
