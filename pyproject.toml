[project]
name = "fxinvestment"
version = "0.1.0"
description = "FX INVESTMENT — TeamAgent FOREX AI dashboard"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
packages = ["teamagent"]
